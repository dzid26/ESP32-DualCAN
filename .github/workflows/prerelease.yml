name: Prerelease

on:
  push:
    branches: [main]
    paths: ['.github/workflows/prerelease.yml']
  workflow_run:
    workflows: ["Firmware CI"]
    types: [completed]
    branches: [main]

jobs:
  validate:
    name: Validate workflow
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - run: echo "prerelease.yml syntax validated"

  publish:
    name: Publish continuous prerelease
    if: github.event_name == 'workflow_run'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Resolve short SHA
        id: sha
        run: echo "short=${SHA:0:7}" >> "$GITHUB_OUTPUT"
        env:
          SHA: ${{ github.event.workflow_run.head_sha }}

      - name: Download firmware artifact
        uses: actions/download-artifact@v4
        with:
          name: firmware-bin
          github-token: ${{ github.token }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Upload to continuous prerelease
        uses: ncipollo/release-action@v1
        with:
          tag: continuous
          name: Continuous build (${{ steps.sha.outputs.short }})
          prerelease: true
          allowUpdates: true
          removeArtifacts: true
          replacesArtifacts: true
          artifacts: firmware.bin
